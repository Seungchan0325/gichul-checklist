# 기출 체크리스트

모의고사와 수능 기출 풀이 상태, 점수, OMR 답안을 관리하는 반응형 웹 앱입니다.

## 실행

```bash
npm install
npm run dev
```

Supabase를 연결하려면 `.env.example`을 `.env.local`로 복사한 뒤 프로젝트 URL과 anon key를 입력하세요. 키가 없는 상태에서도 현재 프론트엔드 데모는 목업 데이터로 동작합니다.
